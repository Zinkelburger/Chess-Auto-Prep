# Node.js dependencies
node_modules/
.npm
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Environment and secrets
.env
*.env

# Data files
game_cache/
imported_games/
tactics_positions.csv

# Python
venv/
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
env/
pip-log.txt
pip-delete-this-directory.txt
.tox/
.coverage
.coverage.*
.cache
nosetests.xml
coverage.xml
*.cover
*.log
.git
.mypy_cache
.pytest_cache
.hypothesis

# IDEs
.vscode/
.idea/
*.swp
*.swo
*~

# OS
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Vite/Frontend build
dist/
.vite/
tactics-trainer/frontend/node_modules/
tactics-trainer/frontend/dist/
tactics-trainer/frontend/.vite/

# flatpak-builder state
.flatpak-builder/

# Flutter/Dart
build/
.dart_tool/
.packages
.pub-cache/
.pub/
*.lock
!pubspec.lock

# Flutter-generated, machine-specific (absolute pub-cache paths)
.flutter-plugins-dependencies
**/flutter/ephemeral/

# Project specific
maia2_models/
lichess_output.txt
*.tmp
output.txt

# Scratch working data — kept on disk, not committed.
# TWIC issue dumps: python/twic-position-finder/downloader.py fetches its own.
twic*.pgn

# tree_builder cdbdirect native build output (bundled via packages/cdbdirect_flutter_libs)
packages/cdbdirect_flutter_libs/native/
packages/cdbdirect_flutter_libs/.build/

# tree_builder
tree_builder/deps/
tree_builder/bin/
tree_builder/*.db
tree_builder/*.db-shm
tree_builder/*.db-wal
tree_builder/local_lib/
tree_builder/*.o
tree_builder/*.json
.lichess_token

# Shared reference artefacts
!tree_builder/analysis_10ply.tree.json
!tree_builder/analysis_10ply.db

# Embedded repos
maia-platform-frontend/

# Generated FEN indexes for PGN collections
*.fenidx

# Stockfish engines -- fetched by `python3 tools/fetch_assets.py`, never committed.
# These are bundled by pubspec.yaml and must exist before `flutter build`.
# assets/maia3_simplified.onnx is deliberately NOT here: it has no upstream to
# fetch from, so git is the only copy.
assets/executables/*.gz

# Regenerable scrape caches (see scripts/chesscom_titled_analysis/fetch_data.py).
# Note: scripts/data/ is deliberately NOT ignored -- it is only ~3 MB and holds
# the player map that build_player_map.py incrementally builds and resumes.
scripts/chesscom_titled_analysis/data/

# Sample TWIC input -- re-fetch with python/twic-position-finder/downloader.py
twic*.pgn

# harness results: trees, sandboxes, logs (keep compare reports)
tools/experiments/fast_vs_pure/results/*/fast/
tools/experiments/fast_vs_pure/results/*/pure/
tools/experiments/fast_vs_pure/results/*/*.log
tools/experiments/fast_vs_pure/results/*/pid

# benko_master harness: keep the scripts, not the runs (trees, engine copies, pids)
tools/experiments/benko_master/run_*/
tools/experiments/benko_master/LATEST*
