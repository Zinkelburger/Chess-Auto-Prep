/// Which detail panes the repertoire builder's context column can show.
library;

/// Context sub-views within the builder's context pane (TabBar).
enum EditContextView { browse, engine, expectimax, lines, tree }
